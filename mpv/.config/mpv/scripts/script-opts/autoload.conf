# autoload.conf
# Customize autoload behavior

enable_videos=yes
enable_audio=yes
enable_images=no
max_entries=5000

# Optional: Override extensions (comma-separated, lowercase)
# extensions=mp4,mkv,webm,avi,mov,flv
